# 作業ルール（これ食っていい）

## このリポジトリについて

- 食べ物の写真を撮ると、その日の残りカロリーと見比べて食べていいか答えるPWAアプリ
- 公開先: https://hamburg1106-collab.github.io/kore-kutte-ii/
- ユーザーは非エンジニア。専門用語は使うときにその場で説明する

## 出力ルール

- 日本語で出力
- 結論から書く
- 箇条書きを優先
- 長文にしない
- 不明点がある場合は作業前に質問

## 安全ルール

- 勝手にファイル削除しない
- 既存ファイルを上書きする前に確認
- コマンド実行前に目的を説明
- **APIキーやパスワードをコードに書かない**（利用者のGeminiキーは端末のlocalStorageにだけ保存する。どこにも送信・同期しない）
- **`main` へのpushは公開サイトに即反映される。** pushの前に必ず確認を取る

## ファイル構成

Vite構成なので、置き場所を間違えると公開時に消える。

| 場所 | 中身 |
|---|---|
| `src/main.js` | アプリ本体。CSSもここからimportしている |
| `src/style.css` | 見た目 |
| `public/` | そのまま配信されるもの（`sw.js` / `manifest.json` / アイコン） |
| `index.html` | 画面の骨格 |
| `dist/` | **ビルド結果。直接編集しない。毎回作り直される** |

## 公開のしかた

- `main` にpushすると**GitHub Actionsが自動でビルドして公開**する。手でビルドする必要はない
- 開発中は `npm run dev`（保存した瞬間に画面が更新される）
- 公開前の最終確認は `npm run build` → `npm run preview`

## 触るときの注意

- **`public/sw.js` の `ASSETS` に、存在しないファイル名を書かない。** 1つでも404だと`cache.addAll`がまるごと失敗し、オフライン機能が停止する。ビルド後のJS/CSSは名前にハッシュが付くので、ここには書けない（読み込んだものを自動でキャッシュする方式になっている）
- アイコンやmanifestを変えたときは `CACHE_NAME` の版数を上げる
- 個人データ（体重・食事）は、Firestoreのパスに推測不能な「同期コード」を挟んで保護している。この仕組みを壊さない
- Firebaseの設定値がコードに直書きされているのは**意図的**（公開前提の値なので問題ない）

## 段階的な移行の途中

大きく書き直さず、3段階で進める方針。

1. **Viteの器に載せ替え（完了）**
2. 型を少しずつ付ける ← いまここ
3. 必要な画面だけReact化

**一気に全部書き直さない。** 移植が終わるまで機能追加ができなくなるため。

## 報告形式

- 変更したファイル
- 変更内容
- 確認したこと
- 次にやること
