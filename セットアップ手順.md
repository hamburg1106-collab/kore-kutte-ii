# 「これ食っていい？」セットアップ手順

スマホとPCで同じ記録を見られるようにするまでの手順。上から順にやる。

---

## 1. Firestoreのセキュリティルールを設定する

体重・年齢を保存するので、ここを先にやる。

1. [Firebaseコンソール](https://console.firebase.google.com/) を開く
2. プロジェクト **ouchi-no-kondate** を選ぶ
3. 左メニュー **構築 → Firestore Database** → 上部タブ **ルール**
4. 今書かれている内容を**メモ帳などに控えておく**（戻せるように）
5. 下のルールを貼り付けて **公開** を押す

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // --- うちの献立（今まで通り） ---
    match /inventory/{doc} { allow read, write: if true; }
    match /recipes/{doc}   { allow read, write: if true; }
    match /history/{doc}   { allow read, write: if true; }

    // --- これ食っていい？ ---
    // 同期コードを知っている端末だけが読み書きできる。
    // healthUsers の一覧は取れないので、コードを知らない人はデータに辿り着けない。
    match /healthUsers/{code}/{sub}/{doc} {
      allow read, write: if code.size() >= 16;
    }
  }
}
```

> **注意**: 今のルールに献立アプリ用の別の記述があれば、それは消さずに残すこと。
> 上の `inventory` / `recipes` / `history` の3行は、献立アプリを今まで通り動かすためのもの。

---

## 2. GitHubにアップロードして公開する

献立アプリ（https://hamburg1106-collab.github.io/ouchi-no-kondate/ ）と同じやり方。

1. [GitHub](https://github.com/new) で新しいリポジトリを作る
   - Repository name: `kore-kutte-ii`
   - **Public** を選ぶ（Privateだと GitHub Pages が使えない）
   - 「Add a README file」はチェックを外してよい
   - **Create repository**
2. 作成後の画面で **uploading an existing file** のリンクを押す
3. `C:\作業スペース\これ食っていい\` の中の**次の7つ**をまとめてドラッグ＆ドロップ
   - `index.html`
   - `style.css`
   - `script.js`
   - `manifest.json`
   - `sw.js`
   - `icon-192.png`
   - `icon-512.png`
   - ※ この `セットアップ手順.md` は上げても上げなくてもよい
4. 下の **Commit changes** を押す
5. 上部タブ **Settings** → 左メニュー **Pages**
   - Source: **Deploy from a branch**
   - Branch: **main** / **/(root)** → **Save**
6. 1〜2分待つと、このURLで開けるようになる

```
https://hamburg1106-collab.github.io/kore-kutte-ii/
```

---

## 3. PCで同期コードを確認する

1. 上のURLをPCのブラウザで開く
2. **設定**タブ → 「端末間の同期」の**同期コード**をコピー
3. 右上のバッジが「端末間で同期中」になっていることを確認
4. 身長・体重・年齢、Gemini APIキーを入れる

---

## 4. スマホで開く

1. スマホのブラウザで同じURLを開く
2. **設定**タブ → 同期コードの欄に、PCでコピーしたコードを貼り付ける
   - 自動で読み込み直され、PCと同じ記録が出れば成功
3. Gemini APIキーもスマホ側で入力する（キーは端末ごとに保存されるため）
4. ブラウザのメニューから **ホーム画面に追加** すると、アプリのように使える

---

## アプリを直したとき

1. GitHubのリポジトリを開く
2. 直したファイルを開いて鉛筆アイコンで編集、または **Add file → Upload files** で上書き
3. `sw.js` の `CACHE_NAME` の数字を1つ増やしておくと、スマホ側に確実に反映される

---

## 困ったとき

| 症状 | 原因と対処 |
|---|---|
| 右上が「この端末に保存」のまま | ファイルを直接開いている（file://）。上のURLから開く |
| 右上が「同期エラー」 | Firestoreのルールが未設定。手順1をやり直す |
| スマホとPCで記録が違う | 同期コードが違う。設定タブで見比べる |
| 判定でキーのエラーが出る | スマホ側にもGemini APIキーを入れる |
| 直したのに反映されない | `sw.js` の `CACHE_NAME` を増やして再アップロード |
